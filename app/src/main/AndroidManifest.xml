<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="group50.coupletones">

    <application
            android:name="CoupleTones"
            android:allowBackup="true"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:supportsRtl="true"
            android:theme="@style/AppTheme">
        <activity android:name=".controller.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity
                android:name=".controller.MainActivity"
                android:label="@string/title_activity_main"
                android:theme="@style/AppTheme">
        </activity>

        <service android:name=".network.gcm.GcmListener"></service>
        android:theme="@style/AppTheme" />
        <activity
                android:name=".controller.EditLocationActivity"
                android:label="@string/title_activity_edit_location"
                android:theme="@style/AppTheme"/>
        <activity
                android:name=".controller.AddPartnerActivity"
                android:label="@string/title_activity_add_partner"></activity>

        <receiver
                android:name=".network.gcm.GcmReceiver"
                android:permission="com.google.android.c2dm.permission.SEND">
            <intent-filter>
                <action android:name="com.google.android.c2dm.intent.RECEIVE"/>
                <category android:name="group50.coupletones"/>
            </intent-filter>
        </receiver>

        <service android:name=".network.gcm.GcmMessageHandler"></service>
		<meta-data
			android:name="com.google.android.geo.API_KEY"
			android:value="AIzaSyA3oZznWLtXHfTY6XSKP2Krbd9rR2nxwi4"/>
	</application>
	
    <uses-permission android:name="com.google.android.providers.gsf.permission.READ_GSERVICES"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
</manifest>
